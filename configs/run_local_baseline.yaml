name: local-llama31-baseline
target: llama3.1-8b-local
defences: []
corpora:
  - source: advbench
    limit: 100          # match the AdvBench x Sonnet cell (n=100)
budget_usd: 0.0          # local => $0
concurrency: 1           # one Ollama call at a time on the 4070
max_tokens: 256
